pragma solidity >=0.4.0 <0.9.0;

/**
 * ABI (Application Binary Interface):
 * Same concept as API but is defined in an array that details all the exposed functions of the contract.
 */
contract MyContract {
    string public myString = "hello world";
}
